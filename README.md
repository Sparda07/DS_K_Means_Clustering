# DS_K_Means_Clustering
This project implements K-Means Clustering, an unsupervised learning algorithm, to partition data into $K$ distinct groups. I focused on:Optimal K: Used the Elbow Method to find the ideal cluster count.Centroid Logic: Iteratively refined cluster centers to minimize Within-Cluster Sum of Squares (WCSS).Successfully identified patterns.
